version: 2
# https://docs.getdbt.com/docs/build/sources

sources:
  - name: gtfs
    description: '{{ doc("stg_gtfs") }}'
    meta:
      external_location: s3://hdpt/raw/gtfs/{name}.txt
    tables:
      - name: calendar_dates
        descrition: '{{ doc("source_calendar_dates") }}'
      - name: routes
        description: '{{ doc("source_routes") }}'
      - name: shapes
        description: '{{ doc("source_shapes") }}'
      - name: stop_times
        description: '{{ doc("source_stop_times") }}'
      - name: stops
        description: '{{ doc("source_stops") }}'
      - name: trips
        description: '{{ doc("source_trips") }}'