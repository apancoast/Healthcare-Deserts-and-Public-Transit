version: 2

seeds:
  - name: seed_census_bureau__dp04_metadata
    config:
      enabled: true
  - name: seed_census_bureau__s2704_metadata
    config:
      enabled: true
  - name: seed_hpsa__components_shapefile__data_catalog
    config:
      enabled: true